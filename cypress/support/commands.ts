// Custom Cypress commands file
// Contains:
// - login(email, password): Command to login
// - getAuthToken(): Get token from localStorage
// - apiRequest(method, endpoint, body): Custom API request
// - Any other custom commands for use in tests
